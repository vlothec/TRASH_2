shift_and_compare = function(sequence, templates = "") {
  shifted = FALSE
  adjusted_sequence = sequence
  if(templates != "") {
    ### Compare ===============================================

    
  } 
  if(!shifted) {
    ### Shift =================================================
  } 
  
  return(adjusted_sequence)
}